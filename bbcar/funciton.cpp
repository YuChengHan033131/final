#include "function.h"
void cali(){
    /*
    encoderL.reset();
    encoderR.reset();
    int pwm=30;
    car.servo0.pwm_value=pwm;
    car.servo0.control();
    while(encoderL.get_steps()<1){}
    Timer t;
    t.start();
    encoderL.reset();
    while(encoderL.get_steps()<5){
        //xbee.printf("%d\r\n",encoderL.get_steps());
        //xbee.printf("ping time:%20f,ping distance:%f\r\npi",ping.ping(),ping.ping_cm()
    }
    xbee.printf("left:%f speed:%d\r\n",6.5*3.14*5/t.read(),pwm);
    car.stop();

    xbee.printf("%f,%f",car.servo0.pwm_value,car.servo1.pwm_value);
    */
}